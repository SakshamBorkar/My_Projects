<div className="relative w-full h-[500px] mb-10"> {/* Adjust height */}
        {data[1].map((canvasdets, index) => (
          <Canvas key={index} details={canvasdets} />
        ))}
</div>

      <div className="relative w-full h-[500px] mb-10"> {/* Adjust height */}
        {data[2].map((canvasdets, index) => (
          <Canvas key={index} details={canvasdets} />
        ))}
      </div>

        <div className="relative w-full h-[500px] mb-10"> {/* Adjust height */}
          {data[3].map((canvasdets, index) => (
            <Canvas key={index} details={canvasdets} />
          ))}
        </div>

        <div className="relative w-full h-[500px] mb-10"> {/* Adjust height */}
          {data[4].map((canvasdets, index) => (
            <Canvas key={index} details={canvasdets} />
          ))}
        </div>

        <div className="relative w-full h-[500px] mb-10"> {/* Adjust height */}
          {data[5].map((canvasdets, index) => (
            <Canvas key={index} details={canvasdets} />
          ))}
        </div>

        <div className="relative w-full h-[500px] mb-10"> {/* Adjust height */}
          {data[6].map((canvasdets, index) => (
            <Canvas key={index} details={canvasdets} />
          ))}
        </div>

        <div className="relative w-full h-[500px] mb-10"> {/* Adjust height */}
          {data[7].map((canvasdets, index) => (
            <Canvas key={index} details={canvasdets} />
          ))}
        </div>

        <div className="relative w-full h-[500px] mb-10"> {/* Adjust height */}
          {data[8].map((canvasdets, index) => (
            <Canvas key={index} details={canvasdets} />
          ))}
        </div>

        <div className="relative w-full h-[500px] mb-10"> {/* Adjust height */}
          {data[9].map((canvasdets, index) => (
            <Canvas key={index} details={canvasdets} />
          ))}
        </div>